# Adsk.Platform.HttpClient

> **⚠️UNOFFICIAL PACKAGE⚠️**

The package `Adsk.Platform.HttpClient` provides a resilient HttpClient serving the Adsk Toolkit SDKs.

## Installation

```bash
dotnet add package Adsk.Platform.HttpClient
```

## Usage

This package is used internally by the other SDK packages. You typically don't need to install it directly — it is included as a dependency.

See the [Custom HttpClient](../HowTo/customizeSDK.md) guide for advanced customization options.
